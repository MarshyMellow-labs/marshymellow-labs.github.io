{
  "name": "marshymellow-control-companion",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Fail-closed local bridge for the Control Marshy website, PiShock, and Pulsoid.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env marshy-control.mjs",
    "check": "node --check marshy-control.mjs && node --check control-core.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
